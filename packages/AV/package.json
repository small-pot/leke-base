{
  "name": "@leke/AV",
  "version": "1.0.0",
  "main": "lib/index.js",
  "module": "es/index.js",
  "scripts": {
    "build:umd": "rimraf dist && rollup --config rollup.config.js",
    "build:modules": "rimraf style && rimraf es && rimraf lib && gulp",
    "build": "npm-run-all --parallel build:*"
  },
  "dependencies": {},
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^11.1.0",
    "gulp-rename": "^2.0.0",
    "rollup": "^2.36.2",
    "rollup-plugin-typescript2": "^0.29.0",
    "rollup-plugin-uglify": "^6.0.4"
  }
}
